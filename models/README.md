# Pretrained Models

The pretrained model weights (`*.pth`) are not included in this repository because of GitHub file size limits.

This directory contains:

- Training history (`history*.json`)
- Validation metrics
- Test-Time Augmentation (TTA) results

The pretrained weights will be made available after the publication of the paper or can be requested from the author.
